{{- $portals := dict
  "portal-dataspace" .Values.portalDataspace
  "portal-tata-admin" .Values.portalTataAdmin
  "portal-tata-public" .Values.portalTataPublic
  "portal-wallet" .Values.portalWallet
  "portal-insurance" .Values.portalInsurance
  "portal-company" .Values.portalCompany
}}
{{- range $name, $val := $portals }}
{{- if $val.ingress.subdomain }}
{{- $host := include "app-chart.host" (dict "subdomain" $val.ingress.subdomain "ctx" $) }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "app-chart.fullname" $ }}-{{ $name }}
  labels:
    {{- include "app-chart.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ $name }}
  annotations:
    haproxy.org/server-ssl: "false"
    {{- if $.Values.ingress.tls }}
    cert-manager.io/cluster-issuer: {{ $.Values.ingress.clusterIssuer | quote }}
    haproxy.org/ssl-redirect: "true"
    {{- end }}
spec:
  ingressClassName: {{ $.Values.ingress.className }}
  {{- if $.Values.ingress.tls }}
  tls:
    - hosts:
        - {{ $host }}
      secretName: {{ $val.ingress.subdomain }}-tls
  {{- end }}
  rules:
    - host: {{ $host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "app-chart.fullname" $ }}-{{ $name }}
                port:
                  number: {{ $val.service.port }}
{{- end }}
{{- end }}
